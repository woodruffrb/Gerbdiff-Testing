name: GerbDiff Visual Review

on:
  pull_request:
    paths:
      - 'gerbers/**'

jobs:
  gerbdiff:
    name: Visual Gerber Diff
    runs-on: windows-latest
    permissions:
      contents: read
      pull-requests: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run GerbDiff
        uses: ./github-action
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          base-path: gerbers
          diff-resolution: 1200
